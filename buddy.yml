- pipeline: "Zurich - Build Kubernetes Cluster"
  trigger_mode: "MANUAL"
  ref_name: "master"
  actions:
  - action: "Bootstrap the OS cluster"
    type: "BUILD"
    docker_image_name: "alpine"
    docker_image_tag: "latest"
    execute_commands:
    - "export OS_AUTH_URL=https://os.ch-ti1.server.one/v3"
    - "export OS_USERNAME=rods"
    - "export OS_REGION_NAME=ch-ti1"
    - "export OS_TENANT_ID=db5218670782412f95eb22be133f96c5"
    - "export OS_PASSWORD=Z8GZgUhiCksn"
    - "export OS_USER_DOMAIN_NAME=Default"
    - "ansible-playbook -i .kargo/inventory --private-key /root/.ssh/k8s -e 'masters_num=2' -e 'nodes_num=3' -e 'masters_flavor=m1.small' -e 'nodes_flavor=m1.large' -e 'image=0d69bf12-e518-43d3-b5e5-5f3369a2d572' -e 'security_groups=default' up.yml -vvvvv"
    setup_commands:
    - "apk update"
    - "apk add python2 py2-pip gcc python2-dev musl-dev libffi-dev"
    - "apk add openssl-dev ca-certificates linux-headers coreutils git"
    - "pip install -U pip"
    - "pip install -U ansible"
    - "pip install -U shade"